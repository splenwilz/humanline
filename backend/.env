# Development Environment Configuration for Humanline Backend
# Copy this to .env for development mode

# Docker Build Configuration
INSTALL_DEV=true
CONTAINER_SUFFIX=-dev

# Database Configuration (Development)
POSTGRES_DB=humanline_dev
POSTGRES_USER=humanline_user
POSTGRES_PASSWORD=humanline_dev_password
POSTGRES_PORT=5433

# Redis Configuration (Development)
REDIS_PASSWORD=redis_dev_password
REDIS_PORT=6380

# JWT Security Configuration (Development - Less Secure)
SECRET_KEY=dev-secret-key-not-for-production-use-only-change-this
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# CORS Configuration (Development - More Permissive)
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001,http://localhost:8001

# Environment (IMPORTANT: Development mode enables docs and hot reloading)
ENVIRONMENT=development

# Server Ports (Different from production to avoid conflicts)
BACKEND_PORT=8001

# Logging (More verbose in development)
LOG_LEVEL=debug

# Database URL for SQLAlchemy (constructed from above components)
DATABASE_URL=postgresql+asyncpg://humanline_user:humanline_dev_password@localhost:5432/humanline_dev
# Alternative SQLite URL for local development (no PostgreSQL required):
# DATABASE_URL=sqlite+aiosqlite:///./humanline_dev.db
